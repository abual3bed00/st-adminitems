Config = {}

-- Treat these job names as "admin" for opening the UI.
-- Adjust to your server logic or switch to proper permission checks if needed.
Config.AdminPermissions = {
    "admin",
    "god"
}

-- Max amount safeguard
Config.MaxAmount = 1000
